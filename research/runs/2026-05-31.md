# 研究运行：2026-05-31

## 运行目标（Run Objective）

执行持续学习反馈闭环实施计划，补齐策略晋升门槛、replay 查询和 replay-aware 子 agent 任务包。

## 重要假设（Assumptions）

- 本次允许修改应用代码，因为用户明确要求根据实施计划继续执行。
- Subagent 工具因额度限制未能执行任务；为避免阻塞，改为在当前会话内按同一计划逐项 TDD 实现。
- 当前工作树已有未提交改动，本次不回滚既有内容，也不提交可能混入既有改动的 commit。

## 已阅读文件（Files Reviewed）

- `docs/superpowers/plans/2026-05-30-continual-learning-feedback-loop.md`
- `balatro_agent/analysis.py`
- `balatro_agent/cli.py`
- `tests/test_analysis.py`
- `scripts/subagent-task.sh`
- `strategy/runs/README.md`
- `README.md`
- `research/memory.md`
- `research/findings.md`

## 已采取行动（Actions Taken）

- 新增 `compare_eval_summaries`，用于比较 baseline 和候选评估摘要并输出晋升判断。
- 新增 `promotion-gate` CLI 和 `scripts/promotion-gate.sh`。
- 扩展 replay 抽取，增加 `decision` 案例和动作参数、候选数、参与 agent 等字段。
- 新增 `load_replay_cases`、`query_replay_cases` 和 `replay-query` CLI。
- 让 `scripts/subagent-task.sh` 可选注入相关 replay 案例。
- 更新 README 和 `strategy/runs/README.md`，记录策略晋升门槛和 replay 查询工作流。
- 更新研究记忆和发现。

## 验证结果（Verification）

- `python3 -m unittest discover -s tests`：30 个测试通过。
- `sh -n scripts/*.sh`：通过。
- `python3 -m balatro_agent --help | rg "promotion-gate|replay-query"`：能列出两个新命令。
- `python3 -m balatro_agent promotion-gate --baseline /tmp/balatro-agent-final/baseline.json --candidate /tmp/balatro-agent-final/candidate.json --cohort dev`：输出 `promote: true`。
- `python3 -m balatro_agent replay-query --replay /tmp/balatro-agent-final/replay.jsonl --phase SHOP --limit 1`：返回 `source: "b"` 的 `error` 案例。
- `git diff --check`：通过。

## 新发现（New Findings）

- 只读持续学习反馈闭环已经具备本地测试覆盖，可以在真实 BalatroBot 可用后接入固定 seed 评估。

## 记忆更新（Memory Updates）

- 记录 `promotion-gate` 和 `replay-query` 作为稳定入口。

## 决策更新（Decision Updates）

无。

## 开放问题（Open Questions）

- 当前默认晋升阻断条件是否需要根据真实 `dev`、`regression`、`heldout` run 调整阈值？
- replay 相关性排序是否需要引入 deck、stake、小丑牌标签和动作相似度？

## 建议的下一次运行（Recommended Next Run）

启动 BalatroBot 后运行 baseline 与候选固定 seed 评估，使用 `summarize-eval`、`promotion-gate` 和 `replay-query` 检查真实日志中的策略退化和高价值失败案例。
