# 研究运行：2026-05-31

## 运行目标（Run Objective）

执行持续学习反馈闭环实施计划，补齐策略晋升门槛、replay 查询和 replay-aware 子 agent 任务包。

## 重要假设（Assumptions）

- 本次允许修改应用代码，因为用户明确要求根据实施计划继续执行。
- Subagent 工具因额度限制未能执行任务；为避免阻塞，改为在当前会话内按同一计划逐项 TDD 实现。
- 当前工作树已有未提交改动，本次不回滚既有内容，也不提交可能混入既有改动的 commit。

## 已阅读文件（Files Reviewed）

- `docs/superpowers/plans/2026-05-30-continual-learning-feedback-loop.md`
- `balatro_agent/analysis.py`
- `balatro_agent/cli.py`
- `tests/test_analysis.py`
- `scripts/subagent-task.sh`
- `strategy/runs/README.md`
- `README.md`
- `research/memory.md`
- `research/findings.md`

## 已采取行动（Actions Taken）

- 新增 `compare_eval_summaries`，用于比较 baseline 和候选评估摘要并输出晋升判断。
- 新增 `promotion-gate` CLI 和 `scripts/promotion-gate.sh`。
- 扩展 replay 抽取，增加 `decision` 案例和动作参数、候选数、参与 agent 等字段。
- 新增 `load_replay_cases`、`query_replay_cases` 和 `replay-query` CLI。
- 让 `scripts/subagent-task.sh` 可选注入相关 replay 案例。
- 更新 README 和 `strategy/runs/README.md`，记录策略晋升门槛和 replay 查询工作流。
- 更新研究记忆和发现。

## 验证结果（Verification）

- `python3 -m unittest discover -s tests`：30 个测试通过。
- `sh -n scripts/*.sh`：通过。
- `python3 -m balatro_agent --help | rg "promotion-gate|replay-query"`：能列出两个新命令。
- `python3 -m balatro_agent promotion-gate --baseline /tmp/balatro-agent-final/baseline.json --candidate /tmp/balatro-agent-final/candidate.json --cohort dev`：输出 `promote: true`。
- `python3 -m balatro_agent replay-query --replay /tmp/balatro-agent-final/replay.jsonl --phase SHOP --limit 1`：返回 `source: "b"` 的 `error` 案例。
- `git diff --check`：通过。

## 新发现（New Findings）

- 只读持续学习反馈闭环已经具备本地测试覆盖，可以在真实 BalatroBot 可用后接入固定 seed 评估。

## 记忆更新（Memory Updates）

- 记录 `promotion-gate` 和 `replay-query` 作为稳定入口。

## 决策更新（Decision Updates）

无。

## 开放问题（Open Questions）

- 当前默认晋升阻断条件是否需要根据真实 `dev`、`regression`、`heldout` run 调整阈值？
- replay 相关性排序是否需要引入 deck、stake、小丑牌标签和动作相似度？

## 建议的下一次运行（Recommended Next Run）

启动 BalatroBot 后运行 baseline 与候选固定 seed 评估，使用 `summarize-eval`、`promotion-gate` 和 `replay-query` 检查真实日志中的策略退化和高价值失败案例。

## 追加运行：真实 Balatro 阶段闭环试跑

### 运行目标（Run Objective）

启动真实 Balatro + Lovely + BalatroBot，跑一个短阶段固定 seed 闭环，并在阶段结束后记录可复查证据。

### 重要假设（Assumptions）

- 用户要求“不同跑完”按“无需跑完全局/全 cohort”理解；本次只跑一个短阶段后汇报。
- 本次不修改应用代码，只更新研究记录和保留真实运行日志。
- 阶段边界采用保守定义：单个 `dev` seed（`AGENT1`）最多 30 步，若提前终局则停止。

### 已阅读文件（Files Reviewed）

- `AGENTS.md`
- `README.md`
- `research/README.md`
- `research/memory.md`
- `research/sources.md`
- `research/questions.md`
- `research/findings.md`
- `research/decisions.md`
- `research/runs/2026-05-30.md`
- `research/runs/2026-05-31.md`
- `scripts/doctor.sh`
- `scripts/start.sh`
- `scripts/eval.sh`
- `scripts/step.sh`
- `balatro_agent/runner.py`
- `balatro_agent/cli.py`
- `balatro_agent/evolution.py`
- `balatro_agent/orchestrator.py`
- `balatro_agent/analysis.py`
- `/Users/suriness/Library/Application Support/Balatro/Mods/balatrobot/balatrobot.json`
- `/Users/suriness/Library/Application Support/Balatro/Mods/balatrobot/balatrobot.lua`
- `/Users/suriness/Library/Application Support/Balatro/Mods/balatrobot/src/lua/settings.lua`
- `/Users/suriness/Library/Application Support/Balatro/Mods/balatrobot/src/lua/core/server.lua`
- `/Users/suriness/Library/Application Support/Balatro/Mods/balatrobot/src/lua/endpoints/health.lua`

### 已采取行动（Actions Taken）

- 运行 `sh scripts/doctor.sh`，确认单元测试通过，但 BalatroBot 未启动时健康检查超时。
- 检查本机 Balatro、Lovely 和 BalatroBot 安装位置，确认 Steam 版 Balatro 和 BalatroBot mod 存在。
- 使用 `run_lovely_macos.sh` 启动真实 Balatro，并确认 `love` 进程监听 `127.0.0.1:12346`。
- 运行 `python3 -m balatro_agent --timeout 3 doctor`，确认 BalatroBot 返回 `{"status": "ok"}`。
- 通过 Computer Use 检查 Balatro 窗口处于真实游戏主菜单。
- 运行单 seed 短阶段评估：`SEEDS=AGENT1 MAX_STEPS=30 LOG_DIR=runs/eval/live-20260531-134011-stage1 sh scripts/eval.sh`。
- 对真实运行日志执行 `summarize-eval` 和 `build-replay`。
- 直接查询终局 `gamestate`，确认真实游戏停在 `GAME_OVER`。
- 更新研究记忆、发现和开放问题。

### 验证结果（Verification）

- `sh scripts/doctor.sh`：30 个单元测试通过；BalatroBot 未启动时健康检查超时。
- Balatro 启动后，`lsof -nP -iTCP:12346 -sTCP:LISTEN` 显示 `love` 监听 `127.0.0.1:12346`。
- `python3 -m balatro_agent --timeout 3 doctor`：返回 `{"status": "ok"}`。
- `SEEDS=AGENT1 MAX_STEPS=30 ... sh scripts/eval.sh`：返回 `game_over_loss`，`steps: 5`，终局 state 为 `phase: "GAME_OVER"`、`won: false`。
- `python3 -m balatro_agent summarize-eval --log-dir runs/eval/live-20260531-134011-stage1`：输出 1 个 run、5 条记录、0 个错误、0 个 rejected，但状态为 `incomplete`。
- `python3 -m balatro_agent build-replay --log-dir runs/eval/live-20260531-134011-stage1 --output strategy/runs/live-2026-05-31-stage1-replay.jsonl`：输出 `case_count: 0`；空 replay 文件未保留。

### 新发现（New Findings）

- 本机真实 Balatro + BalatroBot 已可用，BalatroBot mod 版本为 `1.4.0`。
- 真实 loss 终局返回 `GAME_OVER` 和 `won: false`，支持本地胜负契约的失败侧假设。
- JSONL 决策日志缺少终局状态记录，导致日志汇总和 replay 抽取无法识别这次真实失败局。

### 记忆更新（Memory Updates）

- 记录本机真实 BalatroBot 可启动、版本和健康检查事实。
- 记录 live runner 返回值与 JSONL 日志闭环之间的终局状态缺口。

### 决策更新（Decision Updates）

无。

### 开放问题（Open Questions）

- 是否应在 `Runner.run` 检测 `GAME_OVER` 时写入终局 JSONL 记录？
- 胜利终局是否同样稳定返回 `won: true`？
- 第一轮策略修复是否应先针对 ante 1 手牌阶段连续打对子导致首轮失败的问题？

### 建议的下一次运行（Recommended Next Run）

先修复或补齐终局日志记录，让 `summarize-eval` 和 `build-replay` 能识别真实 `game_over_loss`；然后重跑 `AGENT1` 并用 replay 案例分析首轮手牌策略。
