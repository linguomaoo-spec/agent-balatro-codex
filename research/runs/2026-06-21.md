# 研究运行：2026-06-21（模拟进化与 live 分布评估准备）

## 运行目标

运行 `evolve --sim --sim-log-dir <历史日志目录>`，并对模拟冠军执行多次 live `dev` cohort 分布评估。

## 重要假设

- `runs/eval` 是 README 所示的历史评估根目录，应能作为模拟场景库。
- 只有模拟 fitness 严格优于 baseline 的冠军才有资格进入 live 候选分布评估。
- live 评估必须独占本地 BalatroBot；并行控制会污染同一游戏状态。

## 已阅读文件

- `AGENTS.md`、`README.md`、`research/README.md`、`research/memory.md`、`research/sources.md`、`research/questions.md`、`research/findings.md`、`research/decisions.md`。
- `research/runs/` 中所有既有日志，重点为 `2026-06-20.md`。
- `balatro_agent/cli.py`、`balatro_agent/sim_evolution.py`、`balatro_agent/evolution.py` 与相关 CLI 测试。

## 已采取行动

- 运行 `python3 -m unittest discover -s tests`：179 tests, OK。
- 以 README 指定的 `runs/eval` 运行 `evolve --sim`；CLI 报 `ValueError: no SELECTING_HAND scenarios found in runs/eval`。
- 统计确认该根目录下有 62 个 JSONL、7,343 条记录及 2,500 条 `SELECTING_HAND`；根因是加载函数只扫描直接子文件，历史 JSONL 位于批次子目录。
- 改用直接包含 JSONL 的 `runs/eval/live-20260620-011500`，完成 `--generations 3 --population 8 --random-seed 20260621`；产物位于 `runs/sim-evolution/live-20260621/`。
- 检查冠军、regression gate 与 heldout 输出：冠军与 baseline fitness 完全相同，因此不把该同分变异体作为策略候选。

## 新发现

- 目录扫描与 README 的 `runs/eval` 用法不兼容；该根目录的有效历史场景被非递归 glob 跳过。
- 单一批次模拟没有区分 baseline 与冠军，未产生可晋升候选。

## 失败原因

- 未完成多次 live `dev` 分布评估。原因一：模拟结果没有严格优于 baseline，候选没有进入 live 验证的证据门槛。原因二：本机另有独立 `eval --seeds AGENT1 AGENT2 AGENT3` 进程持续占用同一 BalatroBot，并写入 `runs/eval/live-20260621-dev/AGENT1.jsonl`；在它结束前并行运行会污染游戏状态。日志末尾显示该进程仍在 Ante 4 Big Blind 的 `SELECTING_HAND`，不是已可汇总的完整 cohort。

## 记忆更新

- 已更新：记录非递归场景加载限制，以及本次模拟选择压力不足，避免把同分变异体当作策略进步。

## 决策更新

无。

## 开放问题

- 场景加载应如何递归、分层抽样，才能匹配 README 的 `runs/eval` 目录约定并避免首 50 场景偏置？
- 场景库的适应度函数为什么在 genome 变化后仍完全同分？需要在修复加载范围后用最小对照定位。

## 下次成功率估计

- 修复/绕过场景库覆盖问题后，模拟进化产生与 baseline 严格不同 fitness 的概率估计为 **60%–80%**；依据是当前根目录遗漏了 2,500 条有效手牌场景，而单批次、前 50 场景的选择压力为零。该估计不代表 live 得分提升概率。

## 建议的下一次运行

1. 等待现有 live 进程结束并独占 BalatroBot。
2. 用递归且按批次/seed 分层的历史场景库重新运行模拟进化；仅保留严格优于 baseline 的冠军。
3. 对严格冠军及默认 genome 各运行 3–5 次完整 `dev` cohort，以 `measure` 与分布 gate 比较，而非单次分数。

## 变更文件

- `runs/sim-evolution/live-20260621/`：本次模拟进化产物。
- `research/findings.md`、`research/questions.md`、`research/memory.md`、`research/runs/2026-06-21.md`：本次研究证据与记录。

## Commit

待本轮研究记录提交。

---

# 研究运行：2026-06-21（live 中断审计与自我进化恢复准备）

## 运行目标

审计上轮遗留的 live `dev` 评估，确认它是否可作为自我进化流程的基线；同时识别能在 BalatroBot 不可用时继续推进的最小、可验证流程改进。

## 重要假设

- 不完整 cohort 和基础设施故障不能被解释为策略收益或退化。
- 在未设计确认前，不修改模拟场景加载或策略代码。

## 已阅读文件

- `AGENTS.md`、`README.md`、`research/README.md`、`research/memory.md`、`research/sources.md`、`research/questions.md`、`research/findings.md`、`research/decisions.md`。
- `research/runs/` 全部既有日志，重点为本文件先前的 2026-06-21 运行。
- `balatro_agent/cli.py`、`balatro_agent/sim_evolution.py`、`tests/test_elite.py`。

## 已采取行动

- 检查 `runs/eval/live-20260621-dev/`：只存在 AGENT1、AGENT2 日志；AGENT1 已终局，AGENT2 未完成，AGENT3 未开始。
- 对 AGENT1 终局记录复核：ante 6 `The Manacle`，23705/40000，`won: false`。
- 执行 `python3 -m balatro_agent doctor`，直接观察到 HTTP 502；因此未启动任何竞争性 live 评估。
- 执行 `python3 -m unittest discover -s tests`：179 tests, OK。
- 审核模拟场景加载：`load_scenarios_from_logs` 仅对传入目录使用 `glob("*.jsonl")`，与 `runs/eval/<batch>/*.jsonl` 的实际层级不兼容。

## 新发现

- 当前 `live-20260621-dev` 由基础设施中断造成，不能参与策略评估或候选晋升。详见 `research/findings.md` 的 2026-06-21 条目。

## 失败原因

- 未完成 live cohort 或三次稳定通关验证：BalatroBot 健康检查返回 HTTP 502，且已有批次缺少完整 AGENT2/AGENT3 结果。证据为本轮 `doctor` 直接输出与目录内容。

## 记忆更新

- 已更新：将该批次标记为不可用于策略比较的基础设施中断，避免后续误用。

## 决策更新

无。模拟场景递归/分层加载是合理候选，但仍需用户确认最小设计后才实现。

## 开放问题

- 场景加载应如何递归、分层抽样，才能匹配 README 的 `runs/eval` 约定并避免首批次偏置？
- live eval 是否应显式记录 HTTP 502 为 `infra_error`，并阻止不完整 cohort 晋升？

## 下次成功率估计

- BalatroBot 恢复后，当前默认策略从新局重跑完整 dev cohort 仍为 **0%--5%** 三 seed 通关率；依据是 2026-06-20 完整 dev 的 0/3 胜和本轮 AGENT1 的 ante 6 失败。
- 修复场景库递归与分层抽样后，模拟进化产生严格异于 baseline 的 fitness 候选的概率估计为 **60%--80%**；这是筛选器产出概率，不代表 live 胜率。

## 建议的下一次运行

1. 经设计确认后，以测试驱动方式修复模拟场景库的递归、轮转抽样加载。
2. 用完整 `runs/eval` 重新运行模拟进化；仅允许严格优于 baseline 的冠军进入 live 队列。
3. 待 BalatroBot 可用后，从 `MENU` 独占重跑默认 genome 与候选的完整 dev cohort；再按分层门禁验证 regression/heldout。

## 变更文件

- `research/memory.md`
- `research/findings.md`
- `research/questions.md`
- `research/runs/2026-06-21.md`

## Commit

待本轮研究记录验证并提交。
