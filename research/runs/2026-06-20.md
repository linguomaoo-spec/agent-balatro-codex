# 研究运行：2026-06-20（Agent 决策与记忆说明页）

## 运行目标

创建 `research/agent-decision-memory-overview.html`，以离线、单文件中文页面准确说明当前 agent 的 fixed-seed 胜局、运行时决策链、研究记忆积累，以及这些记忆在当前系统中的作用边界。

## 重要假设

- 页面内容只复述已有的一手仓库证据、代码和运行记录，不引入新的游戏行为观察。
- AGENT1 的 fixed-seed 胜局只能证明该路线已复现，不能推断为默认 agent 已稳定通关或已跨 seed 泛化。

## 已阅读文件

- `AGENTS.md`、`README.md`。
- `research/README.md`、`research/memory.md`、`research/sources.md`、`research/questions.md`、`research/findings.md`、`research/decisions.md`。
- `research/runs/` 既有运行日志，尤指 `research/runs/2026-06-18.md`。
- `balatro_agent/agents/`、`balatro_agent/agents/__init__.py`、`balatro_agent/orchestrator.py`、`balatro_agent/actions.py`、`balatro_agent/runner.py`、`balatro_agent/model.py`、`balatro_agent/search.py`。
- `strategy/README.md`、`strategy/index.md`、`strategy/runs/README.md`、相关手牌、商店与 Joker 策略文档。
- `docs/superpowers/specs/2026-06-20-agent-decision-memory-overview-design.md`、`docs/superpowers/plans/2026-06-20-agent-decision-memory-overview.md`。
- `research/agent-decision-memory-overview.html`。

## 已采取行动

- 交叉核对 `research/findings.md`、`research/decisions.md`、2026-06-11/14/18 运行记录与当前运行时代码，确认页面把“已复现 fixed-seed 胜局”和“未验证稳定通关”分开表述。
- 建立 `research/agent-decision-memory-overview.html` 离线说明页，覆盖胜局路线、`GameState → ActionProposal → validate_action → Runner → JSONL` 决策链、可选 checkpoint beam、记忆闭环、能力边界和证据索引。
- 做 HTML 结构与静态/CSS 合约检查：确认包含关键证据词、响应式/打印/减弱动效规则、键盘可操作的 `details`/`summary`，且未引入外部请求、字体或图片依赖。
- 没有运行 Balatro、没有引入新的 live 观察，也没有修改应用代码、策略规则、genome 或测试。

## 验证结果

- 页面明确标注两次 `won: true` 的 AGENT1、红牌组、白注 fixed-seed 证据，以及 ante 8 前两盲的 55264/50000、83556/75000；同时列出固定 68/63 金阈值的过拟合风险和未完成的 `dev`/`regression`/`heldout` 门禁。
- 静态检查确认页面包含 `validate_action`、`StateValue`、Markdown 作用边界、`dev / regression / heldout` 和离线样式契约；文档没有触发任何游戏动作。
- 用户完成 phase-agent 包拆分后，最终复跑 `python3 -m unittest discover -s tests` 通过 104 项；此前的 `consumable` 导入失败仅保留为本轮历史验证观察，不是当前阻塞。

## 新发现

- 无新的游戏行为、策略收益或通关发现。
- 本轮仅作结构性文档化确认：当前 Python 决策运行时不读取 Markdown 记忆来即时决定动作；Markdown 通过“研究/策略记录 → 人或研究 agent 分析 → 显式 Python 启发式或 genome 变更 → 单元测试与 cohort 评估”的间接闭环发挥作用。代码搜索覆盖 `balatro_agent/agents/`、`orchestrator.py`、`actions.py`、`runner.py`、`model.py`、`search.py`，未发现这些运行时模块读取 `research/*.md` 或 `strategy/*.md` 的路径；`README.md`、`strategy/README.md` 与 `strategy/runs/README.md` 也把 Markdown 定义为证据沉淀与后续实现/评估的输入。这不是新增经验发现，只是对既有仓库结构和说明的汇总。

## 失败原因

无 live run 失败。本轮没有通关目标，也没有执行 live run。

- 历史验证限制（已解除，非本任务回归）：本任务期间运行 `python3 -m unittest discover -s tests` 曾失败，错误为 `ModuleNotFoundError: balatro_agent.agents.consumable`。当时用户已有的未追踪 `balatro_agent/agents/` 包目录在 Python 导入优先级中遮蔽 `balatro_agent/agents.py`，而该包的 `__init__.py` 引入的拆分模块尚不完整。随后用户完成 phase-agent 包拆分，最终复跑通过 104 项；该历史观察不是当前单测阻塞，也不解释为文档回归。相关应用文件不属于本任务、未由本轮修改。

仍保留已知风险：AGENT1 固定 68/63 金阈值可能过拟合；`dev`、`regression`、`heldout` 尚未完成验证；AGENT1 的 Lucky Card 波动尚未归因；checkpoint 搜索的真实运行成本仍高。

## 记忆更新

不更新 `research/memory.md`：本轮没有引入或改变稳定事实、工作假设或重要未知项，只使用既有证据生成说明页。

## 决策更新

无。

## 开放问题

- Delayed/Stencil/Campfire 路线在 `regression` 与 `heldout` seed 上是否仍能保持收益，且不会丢失既有能力？
- AGENT1 重复 live run 的 Lucky Card 现金/分数波动由上游随机、状态重置，还是动作序列消耗随机性导致？
- 如何在不削弱分层门禁的前提下，降低 checkpoint 搜索的分支与 rollout 成本？

## 下次成功率估计

- 对既有 AGENT1 fixed-seed Delayed/Stencil/Campfire 路线再次返回胜局的估计为 **75%–90%**。依据仅是两次历史 `won: true` 的路线复现；这不是 `dev` cohort、`regression`、`heldout` 或稳定通关的概率估计，且 Lucky Card 波动与固定阈值风险仍未量化。

## 建议的下一次运行

先从新局重复运行已知 AGENT1 路线，记录胜负、步数、现金、终局字段及 Lucky Card 相关变化；随后执行 `regression` 与 `heldout` 对照，并在任何策略晋升前量化波动、过拟合和搜索成本。

## 变更文件

- `research/agent-decision-memory-overview.html`：本轮离线说明页。
- `research/runs/2026-06-20.md`：本轮文档汇总运行日志。
- `docs/superpowers/specs/2026-06-20-agent-decision-memory-overview-design.md`：已批准设计说明，本轮仅作参照、未修改。
- `docs/superpowers/plans/2026-06-20-agent-decision-memory-overview.md`：已批准实施计划，本轮仅作参照。

## Commit

待主 agent 完成最终验证后，只提交本轮文件。

---

# 研究运行：2026-06-20（第二轮 — Dev Cohort 评估）

## 运行目标

在当前策略代码（拆分后的 `agents/` 包）下运行 `dev` cohort（AGENT1/AGENT2/AGENT3）固定 seed 评估，确认拆包重构后代码行为是否保持不变，并获取当前 baseline 通关状态。

## 重要假设

- 本轮使用默认 genome（`config/default-genome.json`），不加载任何实验性策略权重。
- 项目瘦身拆分不应改变任何 agent 决策行为；本轮也作为回归验证。

## 已阅读文件

- `CLAUDE.md`、`README.md`
- `research/memory.md`、`research/findings.md`、`research/decisions.md`、`research/questions.md`
- `config/eval-seeds.json`

## 已采取行动

- 启动 Balatro + Lovely + BalatroBot（`run_lovely_macos.sh`）
- 验证 `doctor` 返回 `{"status": "ok"}`
- 执行 `eval --seeds AGENT1 AGENT2 AGENT3 --max-steps 500 --log-dir runs/eval/live-20260620-011500`
- 运行 `summarize-eval`

## 验证结果

### 汇总

| 指标 | 值 |
|------|-----|
| win_count | 0 |
| win_rate | 0% |
| error_count | 0 |
| rejected_count | 0 |
| max_ante | 6 |
| total_steps | 376 |

### 逐 Seed

| Seed | Ante | 盲注 | 得分 | 缺口 | 完成率 | Joker | 步数 |
|------|------|------|------|------|--------|-------|------|
| AGENT1 | 6 | Big Blind | 27950/30000 | 2050 | 93.2% | Blue/Chad/Half/Abstract/Scholar | 152 |
| AGENT2 | 4 | The Wall | 17804/20000 | 2196 | 89.0% | Sly/Supernova/Half/Popcorn/Raised Fist | 100 |
| AGENT3 | 5 | The House | 19984/22000 | 2016 | 90.8% | Banner/Supernova/Raised Fist/Card Sharp (4槽) | 124 |

### 与 evolvedv8 对比

三个 seed 结果与 `evolvedv8`（2026-06-13 最终候选）**完全一致**：

| Seed | v8 得分 | 本轮得分 | 变化 |
|------|---------|----------|------|
| AGENT1 | 27950/30000 | 27950/30000 | 无 |
| AGENT2 | 17804/20000 | 17804/20000 | 无 |
| AGENT3 | 19984/22000 | 19984/22000 | 无 |

这证明项目瘦身（`agents.py` → `agents/` 包拆分、脚本删除、测试精简）没有引入行为回归。

## 新发现

- **发现**：拆分后的 `agents/` 包行为与旧单体 `agents.py` 完全一致；`dev` cohort 三 seed 结果与 `evolvedv8` 逐位匹配，error/rejected 均为 0。
- **证据**：`runs/eval/live-20260620-011500` 日志与 `summarize-eval` 输出；与 `runs/eval/live-20260613-194200-evolvedv8` 对比一致。
- **置信度**：High（高）。三 seed 结果逐位匹配，且无新增错误。
- **影响**：后续策略改动可以安全地在 `agents/` 包中进行。

- **发现**：AGENT3 终局仍有 4/5 Joker 槽，一个空槽未填充。根据 `findings.md` 2026-06-18 记录，这是 Ice Cream 过期留空槽的已知问题。本轮 AGENT3 终局 Joker 为 Banner/Supernova/Raised Fist/Card Sharp，没有 Ice Cream，说明 Ice Cream 在较早 ante 已过期并被替换或直接留空。
- **证据**：终局 `state.joker_keys` 为 `['j_banner', 'j_supernova', 'j_raised_fist', 'j_card_sharp']`，`jokers: 4`。
- **置信度**：High（高）。
- **影响**：AGENT3 距离通关只差 2016 分；填充空槽或提升商店 Joker 购买意愿可能直接促成通关。

## 失败原因

三 seed 均因构筑倍率不足失败：
- **AGENT1**：Blue/Chad/Half/Abstract/Scholar 缺乏 ×Mult 来源。Ante 6 Big Blind 需要 30000 分，仅有 Half (+20 Mult) 和 Abstract (+15 Mult) 作为加法倍率来源，不足以在 Big Blind 加倍需求下过关。
- **AGENT2**：Sly/Supernova/Half/Popcorn/Raised Fist 在 The Wall（2× 需求）前缺乏 ×Mult 或爆发手段。Popcorn 持续衰减（-4 Mult/round），Raised Fist 依赖手牌最低牌值。
- **AGENT3**：Banner/Supernova/Raised Fist/Card Sharp 只有 4 个 Joker，缺乏第五个倍率来源；The House 的首手信息隐藏也削弱了弃牌效率。距离通关仅差 2016 分。

三个 seed 均无执行错误或 rejected 动作，证明基础设施稳定、失败纯粹来自策略强度不足。

## 记忆更新

不更新 `research/memory.md`：本轮没有改变稳定事实或工作假设。结果与 evolvedv8 一致，未引入新假设或推翻旧结论。

## 决策更新

无。

## 开放问题

- AGENT3 空槽问题何时触发？Ice Cream 在哪个 ante/round 过期？商店是否有可替换 Joker 但被跳过？
- 当前默认 genome 的 `cash_reserve: 8.0`、`buy_joker: 1.5`、`joker_replacement_margin: 15.0` 是否过于保守，导致错过早期关键购买？
- 是否应在默认策略中包含显式的空槽惩罚，或动态降低替换门槛？

## 下次成功率估计

- 对当前默认 genome + 策略代码继续返回 0 胜的估计为 **>95%**。本轮结果与 v8 完全一致，说明在当前策略参数下 dev cohort 已趋于稳定（非收敛）。
- AGENT3 距离通关最近（差 2016 分），优先解决空槽问题后最可能首先突破。

## 建议的下一次运行

1. 针对 AGENT3 空槽：分析 AGENT3 日志中 Ice Cream 过期时间点和后续商店可用 Joker
2. 测试降低 `joker_replacement_margin` 或提高 `buy_joker` 对 AGENT3 的影响
3. 对 AGENT1 测试重新引入 Delayed/Stencil/Campfire 路线规则

## 变更文件

- `runs/eval/live-20260620-011500/AGENT1.jsonl`：152 步决策日志
- `runs/eval/live-20260620-011500/AGENT2.jsonl`：100 步决策日志
- `runs/eval/live-20260620-011500/AGENT3.jsonl`：124 步决策日志
- `research/runs/2026-06-20.md`：本轮运行日志（更新）

## Commit

待分析完成后提交。

---

# 研究运行：2026-06-20（第三轮 — 塔罗目标改动退化归因）

## 运行目标

解释共享塔罗目标选择改动后 `dev` 评估变差的直接原因；本轮只分析本地日志和 BalatroBot 文档，不修改应用代码。

## 重要假设

- `runs/eval/live-20260620-011500` 是拆包后、塔罗目标改动前的可用 baseline。
- `runs/eval/live-20260620-033000` 使用共享塔罗目标选择改动后的代码，日志中的 `error` 可用于区分 API 失败和策略质量退化。

## 已阅读文件

- `AGENTS.md`、`README.md`、`research/README.md`、`research/memory.md`、`research/sources.md`、`research/questions.md`、`research/findings.md`、`research/decisions.md` 与近期 `research/runs/` 日志。
- `balatro_agent/agents/booster.py`、`consumable.py`、`tarot_targets.py`、`actions.py`。
- `runs/eval/live-20260620-011500/*.jsonl`、`runs/eval/live-20260620-033000/*.jsonl`。
- 本机 BalatroBot `docs/api.md` 与 `src/lua/endpoints/pack.lua`。

## 已采取行动

- 汇总并对比 baseline 与 post-change `dev` cohort。
- 检索所有 post-change `pack` 错误，核对 `pack` endpoint 的实际 schema 和 Lua 参数读取。

## 新发现

- post-change 评估的 4 次新增错误均为目标塔罗开包时把目标索引传为 `cards`，而 `pack` endpoint 只读取 `targets`。因此上游报告目标数为 0。
- 该 API 映射错误改变了后续金钱、消费品和构筑路径；本次分数退化不能用于判断新的目标排序启发式。

## 失败原因

- AGENT1：Hanged Man 补充包选择失败；终局 25344/40000。
- AGENT2：Death 补充包选择失败；终局 13225/20000。
- AGENT3：Hanged Man 与 Moon 补充包选择失败；终局 11066/16500。
- 证据均来自 `runs/eval/live-20260620-033000/*.jsonl` 的 `BAD_REQUEST`，不是非法提案拒绝或随机波动。

## 记忆更新

- 更新 `research/memory.md`：记录 `use.cards` 与 `pack.targets` 的 endpoint-specific 参数差异，避免将这类评估退化误读为策略退化。

## 决策更新

无。

## 开放问题

- 修复 `BoosterAgent` 参数映射后，新的目标选择能否在 error_count 为 0 的同 cohort 对照中维持或改善基线？

## 下次成功率估计

- 修复参数映射后恢复 `error_count: 0` 的概率为 **95%–99%**；依据是本机 API 文档与 Lua 实现直接指定 `targets`。策略分数改善概率暂不估计，因为本轮结果被 endpoint 失败污染。

## 建议的下一次运行

仅将 `BoosterAgent` 的 `pack` 目标参数改为 `targets`，保留 `ConsumableAgent.use.cards`；先执行完整单测和同一 `dev` cohort，再与 `live-20260620-011500` 比较 error_count、逐 seed 终局和塔罗动作。

## 变更文件

- `research/findings.md`
- `research/questions.md`
- `research/memory.md`
- `research/runs/2026-06-20.md`

## Commit

待本轮研究记录提交。

---

# 研究运行：2026-06-20（单分支无人审核自动进化）

## 运行目标

实现用户确认的单分支连续自改循环：外部模型或工具可修改任意文件，系统自动测试、评估并提交或回滚，无人工审核。

## 已采取行动

- 新增 `auto-evolve` CLI、默认 BalatroBot cohort 评估器与端到端测试。
- 每轮记录起始提交，评估 baseline；运行外部变更命令后执行测试和 `dev`、`regression`、`heldout` 评估。
- 仅在 `dev` 提升且三个 cohort 都通过既有门禁时自动提交；否则 `git reset --hard` 恢复起始提交。

## 新发现

- 三个端到端测试确认：收益候选会提交，无收益候选会回滚，候选评估器失败也会回滚。

## 失败原因

无。本轮未运行真实 BalatroBot 评估；自动进化的真实策略收益仍待后续模型变更命令验证。

## 记忆更新

已更新：记录单分支无人审核自动晋升的稳定入口与门禁。

## 决策更新

已更新：采用单分支、无人审核、任意文件可修改的自动进化循环。

## 开放问题

- 哪个外部模型命令和提示词能在真实 cohort 上稳定产生可晋升候选？

## 下次成功率估计

- 首次真实模型候选在不退化前提下通过三层门禁的概率估计为 **10%–25%**；依据是当前策略对小改动敏感且存在单 seed 波动，但自动回滚已覆盖失败路径。

## 建议的下一次运行

以保守的单一策略改进提示词运行一次 `auto-evolve`，先验证真实 BalatroBot 评估器、模型命令和自动回滚/提交闭环。

## 变更文件

- `balatro_agent/auto_evolution.py`
- `balatro_agent/cli.py`
- `scripts/auto-evolve-evaluate.sh`
- `tests/test_auto_evolution.py`
- `README.md`
- `research/memory.md`
- `research/findings.md`
- `research/decisions.md`
- `research/runs/2026-06-20.md`

## Commit

待本轮最终验证后提交。

---

# 研究运行：2026-06-20（失败终局弃牌余量核查）

## 运行目标

核查现有通关/失败记录中，失败终局是否普遍保留较多弃牌次数。

## 重要假设

- 以 JSONL 的 `terminal: true`、`state.won: false` 为失败终局，而非普通回合的中间状态。
- `state.discards` 与 `state.hands` 是该终局可用资源的直接记录。

## 已阅读文件

- `AGENTS.md`、`README.md`、`research/README.md`、`research/memory.md`、`research/sources.md`、`research/questions.md`、`research/findings.md`、`research/decisions.md` 与既有 `research/runs/` 日志。
- `runs/eval/**/*.jsonl`、`runs/human/**/*.jsonl`。

## 已采取行动

- 逐行解析当前保留的 JSONL，筛选失败终局，按 eval 与人工记录分别统计 `discards` 和 `hands`。

## 新发现

- 33 次 eval 失败中，29 次（87.9%）仍保留至少 3 次弃牌，平均为 3.36；全部以 0 次出牌结束。
- 两份人工失败记录分别剩 2 次、0 次弃牌，样本不足以支持相同结论。

## 失败原因

- 当前 eval 样本指向手牌资源利用不足，但本轮未执行干预，不能仅据此归因为弃牌策略错误或推断人工游玩也有相同模式。

## 记忆更新

- 更新 `research/memory.md`，记录“eval 失败多为出牌耗尽、非弃牌耗尽”的待验证假设。

## 决策更新

无。

## 开放问题

- 在何种分数缺口、目标牌型和 Boss 条件下，应优先弃牌而不是立即出牌？

## 下次成功率估计

- 对“加入受分数缺口与目标牌型约束的弃牌规则，至少改善一个 dev seed 且不让其余 seed 退化”的估计为 30%–45%。依据是 87.9% eval 失败保留 3–4 次弃牌，但历史局部弃牌调整并非普遍有效。

## 建议的下一次运行

先仅对一个近失误 seed 加入“预计当前牌型不足且剩余弃牌 ≥ 1 时追目标牌型”的窄规则，并与现有日志逐步对照；通过后再运行完整 dev cohort。

## 变更文件

- `research/memory.md`
- `research/findings.md`
- `research/questions.md`
- `research/runs/2026-06-20.md`

## Commit

待本轮研究记录验证后提交。

---

# 研究运行：2026-06-20（第五轮 — Tarot 修复后 Dev Cohort 评估）

## 运行目标

在 tarot targeting 修复（`fe48f8c`、`5ef460b`）后的当前 `main` 分支上运行 `dev` cohort 评估，验证：
1. **pack errors 是否归零**（之前 034500 仍有 4 个 error）
2. 当前 score baseline（与 011500 / 034500 对比）

## 重要假设

- 所有提交的修复已正确合并到当前工作区
- 0 error 是修复生效的必要条件；非 zero error 的 cohort 结果不能用来判断策略变化

## 已阅读文件

- `CLAUDE.md`、`README.md`
- `research/memory.md`、`research/findings.md`、`research/decisions.md`、`research/questions.md`
- 既有 `2026-06-20.md` 日志：拆包基线（011500）、塔罗退化归因（033000）、失败终局核查
- `config/eval-seeds.json`

## 已采取行动

- 确认 BalatroBot 运行中（`doctor` → `{"status": "ok"}`）
- 执行 `eval --seeds AGENT1 AGENT2 AGENT3 --max-steps 500 --log-dir runs/eval/live-20260620-145605`
- 汇总结果并与前两轮对比

## 验证结果

### 汇总

| 指标 | 值 |
|------|-----|
| win_count | 0 |
| win_rate | 0% |
| error_count | **0** ✅ |
| rejected_count | 0 |
| max_ante | 6 |

### 逐 Seed 比较

| Seed | Ante | 盲注 | 得分/需求 | 缺口 | 本轮 error | 034500 error |
|------|------|------|-----------|------|------------|--------------|
| AGENT1 | 6 | The Manacle | 25344/40000 | 14656 | **0** ✅ | 1 pack |
| AGENT2 | 4 | The Wall | 13225/20000 | 6775 | **0** ✅ | 1 pack |
| AGENT3 | 5 | Big Blind | 11066/16500 | 5434 | **0** ✅ | 2 pack |

### 错误归零确认

本轮三个 seed 的 **pack error 全部清零**。相比上一轮 `live-20260620-034500`（4 errors）和 `live-20260620-030500`（5 errors），这是 tarot targeting 修复后首次 error-free 的完整 dev cohort。

### 分数对比

与上一轮（034500）的分数逐位相同：AGENT1 25344、AGENT2 13225、AGENT3 11066。

但与拆包基线（011500，即塔罗问题前的 evolvedv8 等价结果）比较，本轮有明显退化：

| Seed | 011500（基线） | 本轮 | 变化 |
|------|---------------|------|------|
| AGENT1 | 27950/30000 | 25344/40000 | -2606 分，不同 blind |
| AGENT2 | 17804/20000 | 13225/20000 | -4579 分，相同 The Wall |
| AGENT3 | 19984/22000 | 11066/16500 | -8918 分 |

注意 011500 在 AGENT1 上只打到 Big Blind（30000），本轮到 The Manacle（40000），但 AGENT2/AGENT3 在相同 blind 上差距更大。

### AGENT2 构筑变化

从长期稳定的 `Sly/Supernova/Half/Popcorn/Raised Fist` 变为 `Misprint/Sly/Juggler/Supernova/Popcorn`，丢失了 Half Joker 和 Raised Fist，换入了 Misprint 和 Juggler。这是 6775 分缺口（vs 基线 2196）的主要原因之一。

## 新发现

- **发现**：tarot targeting 修复在 live dev cohort 上已清零 pack errors。三个 seed 合计 **0 error**。
- **证据**：`runs/eval/live-20260620-20260620-145605/*.jsonl` 与 `summarize-eval` 输出。`error_count: 0`。
- **置信度**：High（高）。
- **影响**：后续评估结果不再被 pack error 污染；error_count 可作为策略变化的有效筛选条件。

- **发现**：修复后的分数（AGENT1 25344、AGENT2 13225、AGENT3 11066）与塔罗退化期间（034500）相同，但与拆包基线（011500: AGENT1 27950、AGENT2 17804、AGENT3 19984）有显著差异。说明修复前和修复后的两个不同 buggy 版本可能因 RNG 路径收敛到了同一组终局分数，而非修复恢复了基线分数。
- **证据**：三组 eval 日志的 `summarize-eval` 输出对比。
- **置信度**：Medium（中）。需要更多重复 run 判断哪些差异来自 RNG 波动、哪些来自构筑路径分歧。
- **影响**：当前 `main` 的 baseline 分数低于 evolvedv8 等效基线；后续策略变化必须与本轮 0-error 结果比较，而非 011500。

## 失败原因

三个 seed 均因构筑强度不足失败，与 011500 基线一致：
- **AGENT1**：Blue/Chad/Half/Abstract/Scholar 缺少 ×Mult 来源，The Manacle 限制手牌数进一步压缩爆发
- **AGENT2**：Misprint/Sly/Juggler/Supernova/Popcorn 缺少稳定倍率核心，The Wall ×2 需求下差 6775 分
- **AGENT3**：Supernova/Joker/Mad/Ice Cream/Raised Fist/Erosion 满 6 槽但缺乏协同 ×Mult 或 Planet 等级

## 记忆更新

不更新 `research/memory.md`：本轮没有改变稳定事实或工作假设。

## 决策更新

无。

## 开放问题

- 为什么 tarot 修复前后的分数与退化期间相同？是 RNG 路径收敛还是策略参数决定的必然结果？
- AGENT2 从 Half/Raised Fist 核心变为 Misprint/Juggler，是 tarot 修复引起的 downstream RNG 变化，还是 AGENT2 本身构筑不稳定的表现？
- 当前 baseline 低于 evolvedv8 等效基线，是否需要重新建立 post-fix 基线，或多次复测取平均？

## 下次成功率估计

- 对当前代码在同 cohort 继续返回 0 error 的估计：**>95%**。
- 对返回 0 win 的估计：**>95%**。分数与前几轮一致，agent 无新的策略增强。

## 建议的下一次运行

1. **多次复测当前 baseline**：在 `dev` 上跑 3-5 次当前代码，评估分数分布和 RNG 波动范围
2. **针对 AGENT2 构筑退化**：检查 Juggler/Misprint 购买路径，判断是否可以恢复 Half/Raised Fist 核心
3. **针对 AGENT3 满 6 槽但分低**：Ice Cream 过期后 Raised Fist + Erosion 的协同效率不足；需要 ×Mult 来源

## 变更文件

- `runs/eval/live-20260620-20260620-145605/AGENT1.jsonl`：137 步决策日志
- `runs/eval/live-20260620-20260620-145605/AGENT2.jsonl`：96 步决策日志
- `runs/eval/live-20260620-20260620-145605/AGENT3.jsonl`：118 步决策日志
- `research/runs/2026-06-20.md`：本轮运行日志（更新）

---

# 研究运行：2026-06-20（架构升级四阶段 — 理论修改）

## 运行目标

直接落地上一轮诊断的四阶段提升计划，**仅做代码与单测，禁止启动 BalatroBot 实测**。
修复"v1–v8 锯齿式进化"的四个根因：测量噪声>>改动、瓶颈是结构性缺×Mult、
无前瞻、胜局不积累。

## 重要假设

- 模拟器用于缺口估算与边际比较，不追求像素级复刻真实得分，覆盖率随需扩展。
- 架构改动未经 live 验证收益，只验证逻辑正确性与历史回算。
- live eval 应降级为仅验证模拟器选出的 top-1。

## 已采取行动

新增 6 个模块 + 71 个单测，全部 175 测试通过：

1. **阶段 1 `balatro_agent/scoring_sim.py`**：纯 Python 单手得分模拟器。复刻 base
   chips/mult + planet level + joker 触发 + steel held mult + blue_joker 牌库缩放
   + hanging_chad 重触发。支持完整卡牌字典与 JSONL 摘要身份串两种输入。
2. **阶段 1b `agents/shop.py::_gap_based_joker_bonus`**：缺口驱动商店评分。ante≥4
   且缺 scaling ×Mult 时，用模拟器边际得分加权，×Mult 牌 ×2 优先。
3. **阶段 0 `balatro_agent/measure.py` + CLI `measure`**：噪声量化 + effect-size
   分布 gate。auto-evolve 接入 `--baseline-eval-dir`。
4. **阶段 2 `balatro_agent/lookahead.py`**：廉价手牌前瞻与弃牌雕塑判断。
5. **阶段 3 `balatro_agent/sim_evolution.py`**：模拟器层进化，决策驱动适应度。
6. **阶段 4 `balatro_agent/elite.py`**：per-seed elite 档案 + 胜局先验
   `commitment_prior`。

## 验证结果

- 模拟器对历史 AGENT1 日志 31 手回算：平均误差 23%（初始 58%），4/31 在 5% 内。
- measure 量化真实噪声：AGENT1 σ=866(3σ=2598)、AGENT3 σ=5817(3σ=17453)——
  证明 v6/v7 AGENT3 的"突破/崩塌"与 AGENT3 差 132 分全是噪声。
- sim_evolution 端到端：3 代×8 个体在真实日志场景库上改善适应度（base=15.12，
  best≥15.12）。
- 全部 175 单测通过。

## 新发现

- AGENT3 的"接近通关差 132 分"在 σ=5817 的噪声下不可信——这是阶段 0 最重要的
  产出，直接推翻了过去几轮"AGENT3 最接近突破"的判断。
- 模拟器最大误差源是 joker 重触发级联（hanging_chad+half+scholar）；补 blue_joker
  牌库缩放与 chad 重触发后，平均误差从 58% 降到 23%，足够支撑边际排序信号。

## 失败原因

无 live run。本轮为理论架构修改。

## 记忆更新

更新 `research/memory.md`：记录四阶段架构升级、关键架构约束（决策驱动适应度）、
噪声量化结论。

## 决策更新

无。

## 开放问题

- 模拟器 Joker 覆盖率需随真实构筑扩展（当前覆盖常见计分牌）。
- 各模块 live 收益未验证：阶段 1b 缺口评分、阶段 2 前瞻、阶段 3 模拟进化
  选出的 top-1 需 live 分布验证。
- elite 档案如何回填已知 AGENT1 胜局路线并供 commitment 状态机消费。

## 下次成功率估计

- 各模块单测与历史回算正确性：>95%。
- 阶段 1b 缺口评分在 live dev 上让 AGENT3 132 分缺口过关的概率：30%–45%
  （瓶颈是真实 ×Mult 牌是否出现在商店，缺口评分只改善选择顺序）。

## 建议的下一次运行

1. 对当前代码跑 3–5 次 dev cohort，用 measure 量化真实波动（验证噪声结论）。
2. live 验证阶段 1b 缺口评分是否改善 AGENT3 空槽/×Mult 选择。
3. 用 sim_evolution 选出 top genome，live 验证。

## 变更文件

- `balatro_agent/scoring_sim.py`（新）
- `balatro_agent/measure.py`（新）
- `balatro_agent/lookahead.py`（新）
- `balatro_agent/sim_evolution.py`（新）
- `balatro_agent/elite.py`（新）
- `balatro_agent/agents/shop.py`（缺口评分）
- `balatro_agent/auto_evolution.py`（分布 gate）
- `balatro_agent/cli.py`（measure 命令、auto-evolve flags）
- `tests/test_scoring_sim.py` `tests/test_measure.py` `tests/test_lookahead.py`
  `tests/test_elite.py` `tests/test_shop_agent.py`（新/扩展）
- `research/memory.md`、`research/runs/2026-06-20.md`

## Commit

待提交。

---

# 研究运行：2026-06-20（三个能力模块的运行时接线）

## 运行目标

完成阶段 2 lookahead、阶段 3 sim_evolution 与阶段 4 elite prior 的运行时接线；只运行本地测试，不启动 BalatroBot。

## 已采取行动

- 在 `HandAgent` 的既有弃牌规划为空时，将 `should_discard_over_play` 的模拟器建议转为 `discard` proposal。
- 将 seed 与可选 `EliteArchive` 从 `Runner` 传播到 `DefaultOrchestrator` 和 `HandAgent`；CLI 支持 `--elite-archive`，`run` 支持 `--seed`。
- 新增 `evolve --sim --sim-log-dir DIR`；它从历史 `SELECTING_HAND` JSONL 创建场景库并运行分层模拟进化，不连接 BalatroBot。
- 增加 lookahead、elite 传播和模拟 CLI 端到端测试。

## 验证结果

- `python3 -m unittest discover -s tests`：179 tests, OK。
- `test_cli.py` 在临时 JSONL 场景库上完成 1 代、2 个体的 `evolve --sim` 并生成 `best_genome.json`。
- 未运行 live eval，因此没有新的胜率或得分结论。

## 新发现

- 原有阶段 4 代码在没有 Joker 信号时提前返回，无法读取 elite prior；现已由 prior 回退分支覆盖。

## 失败原因

无。未做 live 验证是本轮范围约束，不是失败。

## 记忆更新

已更新 `research/memory.md`：记录三个模块已接入运行链及 elite archive 的创建/更新仍未自动化。

## 决策更新

无。

## 开放问题

- elite archive 应由哪一个评估/晋升流程自动从获胜或最佳日志回填并持久化？
- lookahead 与 sim-evolve top-1 是否能在多次 live cohort 的分布门禁下带来可检测提升？

## 下次成功率估计

- 以 `evolve --sim` 选出的 top-1 在一次 live dev cohort 中不增加错误的概率为 **85%–95%**；依据是该路径不改变 RPC 参数，且本轮全量单测通过。得分提升概率暂不估计，必须先建立重复 live baseline。

## 建议的下一次运行

1. 从已清洁的历史 eval JSONL 运行 `evolve --sim`，保留冠军 genome。
2. 对冠军与当前基线做 3–5 次相同 dev cohort，使用 `measure` 与分布 gate 判断收益。
3. 设计 elite archive 的自动回填门禁，避免将失败或过拟合路径写入先验。

## 变更文件

- `balatro_agent/agents/base.py`
- `balatro_agent/agents/hand.py`
- `balatro_agent/orchestrator.py`
- `balatro_agent/runner.py`
- `balatro_agent/evolution.py`
- `balatro_agent/cli.py`
- `tests/test_hand_agent.py`
- `tests/test_cli.py`
- `README.md`
- `research/memory.md`
- `research/findings.md`
- `research/runs/2026-06-20.md`

## Commit

已提交；详见本轮 Git history。
