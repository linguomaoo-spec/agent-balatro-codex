# 研究运行：2026-06-11

## 目标与假设

- 目标：总结此前失败，调整 AGENT1 固定 seed 策略并持续真实测试，尝试完成白注通关。
- 通关判据：BalatroBot 终局 `phase: GAME_OVER` 且 `won: true`。
- 假设：AGENT1 的 `Blue Joker`/`Half Joker` 核心需要一个可扩展倍率来源；先测试 Campfire 销售循环，再用持久 `Supernova` 和 Pair 训练缓解 Boss 后重置。
- 本轮只做研究实验和记录，不修改应用代码；定制策略通过临时 Python orchestrator 运行。

## 已阅读文件

- `AGENTS.md`
- `README.md`
- `research/README.md`
- `research/memory.md`
- `research/sources.md`
- `research/questions.md`
- `research/findings.md`
- `research/decisions.md`
- `research/runs/` 下截至 2026-06-07 的全部运行日志
- `/Users/suriness/.codex/automations/automation/memory.md`
- 与问题直接相关的 `balatro_agent/client.py`、`model.py`、`orchestrator.py`、`runner.py` 和 `actions.py`

## 运行与实验

1. 启动本地 Balatro/Lovely/BalatroBot，`doctor` 返回 `status: ok`。
2. `live-20260611-baseline-recheck` 复现 AGENT1 基线：ante 6 Big Blind，25960/30000。
3. 负向替换：
   - Blue -> Supernova：14040/30000。
   - Blue -> Photograph：14816/22000。
   - Abstract -> Photograph：18922/22000。
4. Campfire 分支：
   - 保护 Campfire 并循环消耗牌：The Manacle 38965/40000。
   - 卖 Scholar、买 Gros Michel：通过 The Manacle，进入 ante 7；Gros Michel 自毁后 Small Blind 14620/35000。
   - 用 Supernova 替换 Scholar，先循环 Juggler：稳定进入 ante 7。
5. ante 7 改进：
   - High Priestess 先使用再卖两张星球牌；卖 Hanging Chad 打开临时 Joker 循环槽。
   - 保存 ante 6 后段现金，Small Blind 通过；Big Blind 初次为 41952/52500。
   - 条件 Pair 训练后进入 Boss；`The Wheel` 无对子时弃五张追 Pair，把结果提高到 68843/70000。
   - Chaos the Clown 先免费重掷再出售，最终以 73069/70000 通过 ante 7 Boss。
6. ante 8 测试：
   - Campfire 重置后首店只有 9 金，Strength/Riff-raff 仅提供 2 次出售，Small Blind 18785/50000。
   - Boss 前保留 Matador 并在 ante 8 卖出，Matador 未在 The Wheel 产生现金；3 次出售后最好为 21128/50000。

## 新发现与失败原因

- Campfire 实际每次出售只增加 X0.25，并在击败 Boss 后重置；ante 7 的高销售数不能直接转化为 ante 8 战力。
- `Blue Joker` 的筹码不可由 Supernova/Photograph 的 additive Mult 替代。
- Pair 训练对 Supernova 有效，但必须避开 `The Arm` 的 Scholar/A 爆发窗口。
- `The Wheel` 前两手直接 High Card 是明确失误；使用弃牌追 Pair 后分数提高约 12034。
- Chaos 免费重掷是稳定经济改进；Matador 在该 Boss 路径没有产生预期现金。
- 重复 fixed-seed run 出现 Lucky Card 约 20 金波动，说明 live 结果仍有随机分支，单局不能代表稳定胜率。
- 最终失败原因：缺少能跨 Boss 重置保留的 X 倍率或足够现金，ante 8 首店无法快速重建 Campfire。

## 记忆与决策

- 更新 `research/memory.md`：记录 AGENT1 最佳已到 ante 8、Campfire 重置瓶颈、Blue 保护和条件 Pair 训练。
- 更新 `research/decisions.md`：保留本轮路线为实验基线，但在真实通关和回归验证前不写入生产策略。
- 更新 `research/questions.md`：新增 ante 8 持久倍率/经济问题和 fixed-seed 随机波动问题。

## 下一轮

- 首选单一假设：在不降低 ante 7 Boss 通过率的前提下，保留一个跨 ante 的 X 倍率 Joker，或提前积累能在 ante 8 首店完成至少 8 次出售的现金。
- 次选：寻找可在 ante 7 商店保留到 ante 8 的持久评分 Joker，比较其收益与最后 1--2 层 Campfire。
- 下一轮成功率估计：8%--15%。依据是本轮已稳定证明可到 ante 7 Boss，并出现一次 ante 8；但 ante 8 仍缺约 2.4 倍首战得分，尚无已验证的持久倍率来源。

## 变更范围

- 仅更新研究记录和自动化记忆。
- 未修改应用代码。
- `runs/eval/live-20260611-*` 保留为未跟踪的一手实验日志，不纳入本轮 commit。

---

## 追加运行：Stencil/Delayed 跨 ante 经济通关

### 目标与假设

- 目标：继续总结 ante 8 失败，逐步调整 AGENT1 策略，直到 BalatroBot 返回 `GAME_OVER` 且 `won: true`。
- 假设：`Joker Stencil` 的持久 X 倍率可替代 Supernova；真正瓶颈不是 ante 7 层数，而是把经济跨 Boss 带到 Campfire 重置后的 ante 8。

### 主要实验

1. `Joker Stencil + Campfire` 保持一个空 Joker 槽后，ante 7 Big Blind 从旧路线推进到 51062/52500；满槽 Stencil 反而在 ante 4 失败。
2. 用 `Egg` 在 `The Arm` 前变现，并逐步提高现金保留下限，先通过 ante 7 Big Blind，再以 86394/70000 通过 `The Wheel`；但 Campfire 重置后 ante 8 Small Blind 仅 27060/50000。
3. 把 ante 5/6 现金保护扩展到 ante 7 后，首次把更多现金带进 ante 8，Small Blind 提高到 40590/50000；20 金保护会让 `The Wheel` 退到 61710/70000，15 金可通过。
4. Uranus/use 与三张 kicker 实验未改善：日志显示被标为 Uranus 的牌实际把 `Straight Flush` 提升到 level 2，Pair 仍为 level 1；kicker 改变抽牌序列后让 Boss 退化。
5. 回查早期日志发现 `Delayed Gratification` 在 ante 2 为购买 `Half Joker` 被默认卖掉。改为卖 `Gluttonous Joker`，ante 3 再卖 `Walkie Talkie`/`Hanging Chad` 保持 Stencil X2，Delayed 持有到 ante 4 Big Blind 前，使资金从旧路线约 48 增至 95。
6. 卖 Delayed 换 `Egg`，ante 5/6 保护 68 金、ante 7 保护 63 金；该路线保持原得分曲线并把 65 金带过 `The Wheel`。ante 8 首店发生一次“9 金不足以支付 10 金重掷”的临时脚本错误；提高重掷安全下限后从当前商店续跑。

### 通关结果

- `runs/eval/live-20260611-agent1-delayed-bank-resume/AGENT1.jsonl`：
  - ante 8 Small Blind：55264/50000。
  - ante 8 Big Blind：83556/75000。
  - Amber Acorn 终局：`phase: GAME_OVER`、`won: true`。
- `python3 -m balatro_agent summarize-eval --log-dir runs/eval/live-20260611-agent1-delayed-bank-resume`：`win_count: 1`、`status: game_over_win`、error/rejected 均为 0。
- 注意：终局同时记录 17680/100000，需要下一轮独立复现并核对 BalatroBot 的最终 Boss 分数报告语义。

### 失败原因变化

- 已解决：ante 8 Campfire 重置后现金不足。
- 新风险：固定金额阈值明显针对 AGENT1 商店序列；尚未证明可重复或跨 seed 泛化。
- 基础设施：重掷成本会随商店内重掷次数上升，固定 8 金判断会产生非法动作；本轮只在临时实验脚本中提高安全下限，未修改应用代码。

### 下一轮

- 从全新游戏状态复现 AGENT1 winning route，并核对 Amber Acorn 终局状态。
- 复现后把 68/63 金硬阈值改成动态预算，再测试 regression/heldout。
- 下一轮 AGENT1 单 seed 成功率估计：55%--70%。依据是已有一次直接胜局且前七个 ante 路线稳定，但 live 随机分支、终局分数异常和重掷成本仍带来不确定性。

---

## 追加运行：全新状态复现通关

### 目标与假设

- 目标：从全新 `MENU` 状态独立复现 AGENT1 的 Delayed/Stencil/Campfire 胜局，并核对 Amber Acorn 终局。
- 假设：如果前一轮胜局不是续跑偶然，那么相同 fixed-seed 策略应再次形成 Stencil/Campfire 核心、把现金带入 ante 8，并返回 `won: true`。
- 为避免覆盖工作区既有未提交代码，在 `/tmp/agent-balatro-repro-20260611` 的 detached worktree 上使用提交 `0d0e0ad`；临时实验脚本从上一轮 Codex 会话记录恢复，未写入应用代码。

### 已采取行动

- 读取全部研究文件、全部历史运行日志和自动化记忆。
- 检测并等待已有 `live-20260611-225926-consumable-boost` 评估释放 BalatroBot；该候选仍为 0/3 胜，AGENT1 29388/30000、AGENT2 8930/11000、AGENT3 17413/22000。
- 在隔离 worktree 运行 116 个基线测试，全部通过；临时策略脚本通过 `py_compile`。
- 从全新游戏状态运行 AGENT1，共 264 步，日志写入 `runs/eval/live-20260611-agent1-delayed-bank-repro/AGENT1.jsonl`。

### 验证结果

- `summarize-eval`：`win_count: 1`、`status: game_over_win`、`max_ante: 8`、error/rejected 均为 0。
- ante 7 Boss：90508/70000，结算时 68 金。
- ante 8 Small Blind：55264/50000；Big Blind：83556/75000。
- Amber Acorn：`phase: GAME_OVER`、`won: true`、17680/100000；与上一轮终局字段完全一致。

### 新发现与失败原因

- 新发现：Delayed/Stencil/Campfire 路线可从全新状态复现，fixed-seed 胜局不依赖上一轮续跑状态。
- 新发现：Amber Acorn 的 `won: true` 与 17680/100000 组合可重复；当前安装版本必须以 `won` 判断胜负。
- 失败原因：无，本轮复现目标已通关。未解决风险是固定 68/63 金阈值仍可能过拟合 AGENT1 商店序列。

### 下一轮

- 把 68/63 金硬阈值替换为基于剩余 Boss、当前 Campfire 倍率、重掷成本和预计可出售牌数量的动态预算。
- 先要求 AGENT1 继续通关，再跑 regression/heldout，确认策略不是单 seed 过拟合。
- 下一轮 AGENT1 单 seed 成功率估计：75%--90%。依据是相同路线已取得两次 `won: true`，其中一次为全新状态完整复现；不确定性主要来自 Lucky Card 波动和动态预算改动风险。
