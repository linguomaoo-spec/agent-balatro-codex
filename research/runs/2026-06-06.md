# 研究运行：2026-06-06

## 运行目标（Run Objective）

总结近期失败经验，调整自动 agent 的小丑牌策略，并重新运行真实 BalatroBot 固定 seed 测试，逐步提高通关概率。

## 重要假设（Assumptions）

- “通关”按 BalatroBot 终局 `phase: "GAME_OVER"` 且 `won: true` 判断。
- 本次允许修改策略代码，因为用户明确要求调整游玩策略并重新测试。
- 当前优先使用 `dev` cohort（AGENT1..AGENT3）作为快速反馈；未通关时保留失败证据和下一轮假设。
- 工作区已有本轮开始前的未提交改动；本次不回滚它们，提交时尽量只纳入与策略/研究运行相关的文件。

## 已阅读文件（Files Reviewed）

- `AGENTS.md`
- `README.md`
- `research/README.md`
- `research/memory.md`
- `research/sources.md`
- `research/questions.md`
- `research/findings.md`
- `research/decisions.md`
- `research/runs/2026-05-30.md`
- `research/runs/2026-05-31.md`
- `research/runs/2026-06-01.md`
- `research/runs/2026-06-02.md`
- `balatro_agent/agents.py`
- `balatro_agent/actions.py`
- `balatro_agent/model.py`
- `balatro_agent/evolution.py`
- `balatro_agent/runner.py`
- `balatro_agent/orchestrator.py`
- `tests/test_orchestrator.py`
- `tests/test_evolution.py`
- `tests/test_model.py`
- `runs/eval/live-20260606-current-dev-v3/*.jsonl`
- `/Users/suriness/Library/Application Support/Balatro/Mods/lovely/log/lovely-2026.06.06-16.07.36.log`

## 已采取行动（Actions Taken）

- 复盘 `live-20260606-current-dev-v3`：AGENT1 到 25960/30000，AGENT2 到 3130/4000，AGENT3 卡在 Psychic Boss 的 3796/4000。
- 定位 AGENT3 Psychic 失败：旧策略在 `The Psychic` 下打 4 张两对，得分保持 0。
- 为 Psychic Boss 添加必须打 5 张牌的策略覆盖，并用单元测试固定。
- 定位 live eval 启动崩溃：`start` 后过早 `menu()` 可触发 Balatro Lua 侧 `screenwipe` nil 崩溃；增加 start 后等待离开 `MENU` 的测试和防护。
- 测试中后期 Joker 替换：
  - 保留 plain `Joker` -> `Banner`/`Gros Michel` 的满槽替换规则。
  - 撤回 `Banner` 后强推 `Gros Michel` 替换 `Mad Joker` 并保护 `Supernova` 的假设，因为 live 单 seed 退化。
- 运行真实 BalatroBot 评估：
  - `runs/eval/live-20260606-agent3-startwait`
  - `runs/eval/live-20260606-agent3-banner-gros`
  - `runs/eval/live-20260606-agent3-gros-replace`
  - `runs/eval/live-20260606-dev-banner-gros`
- 从最终完整 dev 日志生成 replay：`strategy/runs/live-2026-06-06-dev-banner-gros-replay.jsonl`。
- 更新研究记忆、发现、决策和开放问题。

## 验证结果（Verification）

- `python3 -m unittest tests.test_evolution`：8 个测试通过。
- `python3 -m unittest discover -s tests`：102 个测试通过。
- `python3 -m balatro_agent summarize-eval --log-dir runs/eval/live-20260606-dev-banner-gros`：
  - `run_count: 3`
  - `win_count: 0`
  - `loss_count: 3`
  - `max_ante: 6`
  - `error_count: 0`
  - `rejected_count: 0`
  - AGENT1：25960/30000，ante 6 round 17
  - AGENT2：3130/4000，ante 3 round 9
  - AGENT3：17928/22000，ante 5 round 15

## 新发现（New Findings）

- Psychic Boss 必须打 5 张牌是 AGENT3 从 ante 3 推进到 ante 5 的关键修正之一。
- `Banner` 替换 plain `Joker` 能显著改善 AGENT3 中后期表现：AGENT3 从 `live-20260606-current-dev-v3` 的 3796/4000 推进到 `live-20260606-dev-banner-gros` 的 17928/22000。
- `Gros Michel` 替换 `Mad Joker` 的强推假设会退化到 ante 4 的 6936/7500，不能保留为稳定规则。
- 当前 `dev` 失败仍不是动作合法性问题；最终完整 cohort 无 error、无 rejected。

## 失败原因（Failure Cause）

- 未达成通关。主要失败原因仍是策略强度不足：
  - AGENT1 缺少 ante 6 之后的中后期倍率/构筑强度，差 4040 分。
  - AGENT2 仍被早期弱 Joker 组合锁死，ante 3 差 870 分。
  - AGENT3 已恢复到 ante 5，但 round 15 仍差 4072 分，可能缺 X 倍率、稳定倍率或更好的后期替换时机。
- 证据来源：`runs/eval/live-20260606-dev-banner-gros/*.jsonl` 和对应 `summarize-eval` 输出。

## 记忆更新（Memory Updates）

- 更新 `research/memory.md`：记录当前最佳 `dev` 结果、Psychic/Banner 修正收益、以及 `Gros Michel` 替换 `Mad Joker` 的负向反例。

## 决策更新（Decision Updates）

- 更新 `research/decisions.md`：保留 plain `Joker` -> `Banner`/`Gros Michel` 替换规则；撤回强推 `Gros Michel` 替换 `Mad Joker` 的静态规则。

## 开放问题（Open Questions）

- AGENT2 的 ante 3 失败是否应优先通过更激进卖弱 Joker、早期 reroll 预算，还是避免购买低成长 chip Joker 解决？
- AGENT1 ante 6 失败更需要 X 倍率 Joker、牌型等级、还是更强的触发牌保护？
- AGENT3 ante 5 round 15 的 17928/22000 主要应靠稳定倍率、X 倍率、牌型等级，还是后期减少无效 reroll 解决？

## 下次成功率估计（Next Success Estimate）

- 下一轮若继续只在 `dev` cohort 上迭代，估计出现至少 1 个 seed 通关的概率为 8%-15%。依据：AGENT1 已到 ante 6 且 AGENT3 恢复到 ante 5，但 AGENT2 仍卡早期，三个 seed 都还存在明确构筑强度缺口。

## 建议的下一次运行（Recommended Next Run）

优先分析 `runs/eval/live-20260606-dev-banner-gros/AGENT2.jsonl` 的 ante 2-3 商店决策，针对弱 Joker 满槽组合设计一条最小替换/重掷规则；同时检查 AGENT1 ante 6 是否缺 X 倍率来源或牌型等级。

## 追加运行：商店纪律与低价值占槽牌

### 运行目标（Run Objective）

总结上一轮失败经验，降低低即时战力 Joker 的购买倾向，重新测试是否能提高 `dev` seed 通关概率。

### 重要假设（Assumptions）

- 本次允许修改策略代码，因为用户明确要求调整游玩策略并重新测试。
- `Red Card`、`Rocket`、`Hallucination` 和纯经济 Joker 在当前策略没有配套计划时会占用早期 Joker 槽。
- 真实 BalatroBot 命令必须设置 `NO_PROXY=127.0.0.1,localhost`，否则本机代理可能把本地请求变成 502。

### 已采取行动（Actions Taken）

- 复盘后续单 seed 日志，确认部分候选买入 `Red Card`、`Rocket`、`Hallucination` 等低即时战力牌后退化。
- 新增测试：
  - `test_shop_prefers_direct_scoring_joker_over_red_card_without_pack_plan`
  - `test_shop_does_not_buy_rocket_over_planet_or_saving_cash`
- 在 `ShopAgent` 中降低 `Red Card`、`Rocket`、`Hallucination`、`To the Moon`、`Golden Joker`、`Cloud 9`、`Business Card`、`Faceless Joker` 等低即时战力或纯经济 Joker 的静态分。
- 试过“晚期满槽且高现金时提高 reroll”的候选，但 live AGENT3 未改善，已从代码和测试中撤回。
- 记录 live 基础设施问题：未设置 `NO_PROXY` 时 doctor 可能出现代理 502；一次 AGENT2 eval 因 Lovely `attempt to index field 'shop' (a nil value)` 崩溃中断。

### 验证结果（Verification）

- `python3 -m unittest discover -s tests`：104 个测试通过。
- `runs/eval/live-20260606-2304-shop-discipline/AGENT1.jsonl`：
  - AGENT1：`game_over_loss`，ante 6 round 17，25960/30000。
  - 同目录 AGENT2 因 Balatro/Lovely 崩溃中断，不能作为策略结果。
- `python3 -m balatro_agent summarize-eval --log-dir runs/eval/live-20260606-2320-shop-discipline-retry`：
  - AGENT2：`game_over_loss`，ante 3 round 9，3506/4000，error 0，rejected 0。
  - AGENT3：`game_over_loss`，ante 5 round 15，17413/22000，error 0，rejected 0。
- `python3 -m balatro_agent summarize-eval --log-dir runs/eval/live-20260606-2332-shop-discipline-final-agent3`：
  - AGENT3：`game_over_loss`，ante 5 round 15，17413/22000，error 0，rejected 0。

### 新发现（New Findings）

- 降低低即时战力 Joker 静态分没有带来通关；AGENT1 持平上一轮最佳，AGENT2 有小幅改善，AGENT3 退化。
- 无明确目标的晚期满槽重掷不是可靠改进，应避免晋升。
- 本地代理和 Balatro/Lovely 崩溃会制造不可判读 eval，需要在后续 runner 或脚本层显式处理。

### 失败原因（Failure Cause）

- 未达成通关。策略侧主要仍是构筑强度不足：
  - AGENT1 缺 ante 6 后的倍率/X 倍率，仍差 4040 分。
  - AGENT2 仍是弱牌型限定 Joker 满槽，最终 3506/4000，差 494 分。
  - AGENT3 仍缺中后期倍率，最终 17413/22000，差 4587 分。
- 基础设施侧，`live-20260606-2304-shop-discipline` 的 AGENT2 被 Lovely `shop` nil 崩溃污染，已用重启后的 retry 目录替代判读。

### 记忆更新（Memory Updates）

- 更新 `research/memory.md`：记录低即时战力 Joker 降权的有限结果、晚期无目标 reroll 反例、本地代理和 Lovely 崩溃风险。

### 决策更新（Decision Updates）

- 更新 `research/decisions.md`：保留低即时战力 Joker 降权；不保留宽泛晚期满槽高现金 reroll 规则。

### 开放问题（Open Questions）

- AGENT2 弱牌型限定 chip Joker 满槽时，是否应优先保留现金找倍率/X 倍率，而不是继续堆 `Clever`/`Sly`/`Droll`/`Zany`？
- AGENT3 的 17413/22000 退化主要来自商店路径、牌型选择、`Ice Cream` 消耗，还是 `The House` 信息隐藏？
- runner 是否应把连续连接错误和 Lovely 崩溃标记为 `infra_error`？

### 下次成功率估计（Next Success Estimate）

- 下一轮若继续只在 `dev` cohort 上迭代，估计至少 1 个 seed 通关概率为 6%-12%。依据：本轮没有净提升，AGENT1 仍接近但缺 4040 分，AGENT2 只差 494 分但构筑弱，AGENT3 从 17928 退到 17413。

### 建议的下一次运行（Recommended Next Run）

优先针对 AGENT2 设计“弱 chip Joker 满槽时保留现金/找倍率”的最小规则，并用 AGENT2 单 seed 验证是否越过 4000；同时不要再扩大无目标 reroll。
