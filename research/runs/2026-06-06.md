# 研究运行：2026-06-06

## 运行目标（Run Objective）

总结前序失败经验，调整小丑牌 agent 的游玩策略和 live 控制流，再用真实 BalatroBot 固定 seed 重新测试，逐步提高通关概率。

## 重要假设（Assumptions）

- 本次允许修改应用代码，因为用户明确要求调整策略并重新测试。
- 优先使用已有 `dev` cohort（AGENT1..AGENT3）做快速 live 迭代；未通关前不声明策略晋升。
- 运行前工作区已有未提交改动，本次不回滚既有内容，并在提交时避免混入无法明确归属的旧改动。
- live 评估结论只在 BalatroBot 服务稳定、动作无 rejected/error 时作为策略证据。

## 已阅读文件（Files Reviewed）

- `AGENTS.md`
- `README.md`
- `research/README.md`
- `research/memory.md`
- `research/sources.md`
- `research/questions.md`
- `research/findings.md`
- `research/decisions.md`
- `research/runs/2026-05-30.md`
- `research/runs/2026-05-31.md`
- `research/runs/2026-06-01.md`
- `research/runs/2026-06-02.md`
- `balatro_agent/agents.py`
- `balatro_agent/evolution.py`
- `balatro_agent/model.py`
- `balatro_agent/runner.py`
- `balatro_agent/analysis.py`
- `tests/test_model.py`
- `tests/test_orchestrator.py`
- `tests/test_evolution.py`
- `tests/test_runner.py`
- `scripts/eval.sh`
- `config/eval-seeds.json`
- `runs/eval/live-20260601-close-last-hand-dev/*.jsonl`
- `runs/eval/live-20260601-banner-dev/*.jsonl`

## 已采取行动（Actions Taken）

- 复盘 2026-06-01 的 `dev` 失败：AGENT1/AGENT3 曾推进到 ante 5，但 AGENT2 稳定卡 ante 3，AGENT3 仍未通关。
- 启动 Steam、Balatro、Lovely 和 BalatroBot，并用 `doctor`、`gamestate` 和 `eval` 确认 live 服务状态。
- 修正 live run factory：如果 `gamestate` 已经是 `MENU`，不再强制调用可能超时的 `menu` 端点。
- 增加弱满槽小丑牌 reroll 压力：当 ante >= 3、现金足够、槽位已满且持有多个弱小丑牌时，shop agent 更愿意 reroll 而不是直接下一轮。
- 让 `GameState` 解析当前 blind 名称，兼容 `blinds` map 中 `status: CURRENT` 的结构。
- 让 hand agent 在 Boss `The Psychic` 下只枚举 5 张出牌，并允许两对/三条/高牌等牌型带 kicker。
- 将 `PLAY_TAROT` 加入 runner transient phase，避免使用星球牌后进入 fallback 循环。
- 用单元测试覆盖上述控制流和策略约束。
- 运行真实 BalatroBot `dev` cohort 和 AGENT3 单 seed 复测，收集新的失败证据。

## 验证结果（Verification）

- `python3 -m unittest tests.test_evolution`：6 个测试通过。
- `python3 -m unittest tests.test_orchestrator`：通过。
- `python3 -m unittest tests.test_model tests.test_orchestrator`：59 个测试通过。
- `python3 -m unittest tests.test_runner`：6 个测试通过。
- `python3 -m unittest tests.test_model tests.test_orchestrator tests.test_evolution`：65 个测试通过。
- `python3 -m unittest discover -s tests`：98 个测试通过。
- `sh -n scripts/*.sh`：脚本语法检查通过。
- `COHORT=dev LOG_DIR=runs/eval/live-20260606-current-dev-v3 sh scripts/eval.sh`：3 个 seed 完成，0 胜，0 error，0 rejected。
- `python3 -m balatro_agent summarize-eval --log-dir runs/eval/live-20260606-current-dev-v3`：AGENT1 ante 6 25960/30000，AGENT2 ante 3 3130/4000，AGENT3 ante 3 3796/4000。
- `SEEDS=AGENT3 LOG_DIR=runs/eval/live-20260606-psychic-agent3-v2 sh scripts/eval.sh`：修正 `blind_name` 和 `The Psychic` 后越过原 ante 3 boss 失败点，但在 ante 4 使用 `Jupiter` 后暴露 `PLAY_TAROT` transient 问题。

## 新发现（New Findings）

- 当前候选策略仍未通关 `dev` cohort，但 AGENT1 从此前 ante 5 推进到 ante 6 的 25960/30000。
- AGENT3 本轮退化的直接原因之一是 `The Psychic` boss 要求 5 张牌，旧逻辑会出 4 张两对并得 0 分。
- 使用星球牌后出现的 `PLAY_TAROT` 阶段需要作为 transient 处理，否则 runner 会进入 fallback 循环并可能回到菜单。
- BalatroBot live 评估需要先确认 Steam 和菜单状态稳定；否则 `menu`/`start` 超时或 502 会让 run 不可判读。

## 失败原因（Failure Cause）

- 策略层面：`dev` cohort 仍无胜局；AGENT2 仍在 ante 3 分数不足，AGENT3 在完整 cohort 中因 `The Psychic` 五张出牌约束缺失而失败。
- 控制流层面：修正 AGENT3 的 boss 出牌后，单 seed 复测又暴露 `PLAY_TAROT` transient 未处理；处理后未能完成稳定 live 复测，因为 BalatroBot 后续出现 502/菜单启动不稳定。
- 证据来源：`runs/eval/live-20260606-current-dev-v3/*.jsonl`、`runs/eval/live-20260606-psychic-agent3-v2/AGENT3.jsonl`、本地 BalatroBot 命令输出和相关测试。

## 记忆更新（Memory Updates）

- `research/memory.md` 新增 2026-06-06 当前候选结果：AGENT1 推进、AGENT2 小幅改善、AGENT3 退化。
- `research/memory.md` 更新 AGENT3 当前最小失败点：先复测 `The Psychic` 和 `PLAY_TAROT` 修正，再回到 ante 5 近失误分析。
- `research/memory.md` 新增 live 基础设施稳定性未知项。

## 决策更新（Decision Updates）

无。

## 开放问题（Open Questions）

- 修正 `The Psychic` 五张出牌和 `PLAY_TAROT` transient 后，AGENT3 是否能恢复到 ante 5+？
- AGENT2 是否因为过早购买低等级星球牌而缺少现金替换弱满槽小丑牌？
- runner 是否应把 active run 意外回到 `MENU` 记录为基础设施失败？
- 还有哪些 BalatroBot phase 应作为 transient phase 处理？

## 下次成功率估计（Next Success Estimate）

- 若下一轮先做 AGENT3 单 seed 复测并确认 `PLAY_TAROT` 修正，成功越过当前 ante 3/4 阻塞点的概率估计为 60%-70%。依据：`The Psychic` 失败已有明确复现和测试覆盖；`PLAY_TAROT` 也已有单元测试覆盖；主要风险是 BalatroBot live 服务不稳定和后续 ante 仍有未知 boss/商店失败。
- 若直接完整通关 `dev` cohort，估计成功率为 15%-25%。依据：AGENT1 已接近中后期，但 AGENT2 和 AGENT3 仍有明确未解决失败模式，且本轮没有完成修正后的完整 live rerun。

## 建议的下一次运行（Recommended Next Run）

先在 Steam 已启动、BalatroBot `doctor` 正常的情况下重跑 `SEEDS=AGENT3 LOG_DIR=runs/eval/live-20260606-psychic-agent3-v3 sh scripts/eval.sh`，确认 `PLAY_TAROT` 修正是否能稳定越过 ante 4；若通过，再重跑完整 `dev` cohort，并优先分析 AGENT2 ante 3 前的星球牌购买、现金保留和弱小丑牌替换。
