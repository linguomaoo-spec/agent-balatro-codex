# 研究运行：2026-06-07

## 运行目标（Run Objective）

总结上一轮 AGENT2 失败经验，验证当前工作区候选是否能提高 AGENT2 通关概率，并继续推进 dev seed 通关。

## 重要假设（Assumptions）

- “通关”仍按 BalatroBot 终局 `phase: "GAME_OVER"` 且 `won: true` 判断。
- 当前工作区已有未提交应用代码和测试改动；本轮不回滚、不改写这些既有改动，只把它们当作待验证候选。
- 本轮优先验证 AGENT2，因为上一轮 AGENT2 只差 494 分但仍被弱 Joker 满槽构筑限制。
- 真实 BalatroBot 命令必须设置 `NO_PROXY=127.0.0.1,localhost`，避免本地代理干扰。

## 已阅读文件（Files Reviewed）

- `AGENTS.md`
- `README.md`
- `research/README.md`
- `research/memory.md`
- `research/sources.md`
- `research/questions.md`
- `research/findings.md`
- `research/decisions.md`
- `research/runs/2026-05-30.md`
- `research/runs/2026-05-31.md`
- `research/runs/2026-06-01.md`
- `research/runs/2026-06-02.md`
- `research/runs/2026-06-06.md`
- `balatro_agent/agents.py`
- `balatro_agent/evolution.py`
- `balatro_agent/runner.py`
- `balatro_agent/client.py`
- `balatro_agent/orchestrator.py`
- `tests/test_orchestrator.py`
- `runs/eval/live-20260606-dev-banner-gros/AGENT2.jsonl`
- `runs/eval/live-20260606-2320-shop-discipline-retry/AGENT2.jsonl`
- `runs/eval/live-20260607-agent2-manual-wait/AGENT2.jsonl`
- `/Users/suriness/Library/Application Support/Balatro/Mods/lovely/log/lovely-2026.06.07-09.39.33.log`

## 已采取行动（Actions Taken）

- 读取 automation 记忆，确认上一轮最佳可判读结果仍为 `dev` 0 胜，AGENT2 卡在 3506/4000。
- 检查当前工作区候选，发现已有未提交策略改动包含 AGENT2 弱满槽 15 金重掷测试；本轮未修改应用代码。
- 运行 `python3 -m unittest discover -s tests`，105 个测试通过。
- 启动真实 Balatro + Lovely + BalatroBot，并用 `NO_PROXY=127.0.0.1,localhost` 确认健康检查通过。
- 尝试 `SEEDS=AGENT2` live eval；`menu`/`start`/首个 `select` 路径多次出现 `Remote end closed connection` 或 `select() called with no blind on deck`，未形成可判读 eval。
- 改用手动等待流程：在 `start` 后等待，再从 `BLIND_SELECT` 交给 `Runner`，生成 `runs/eval/live-20260607-agent2-manual-wait/AGENT2.jsonl`。
- 在验证结束后停止本轮启动的 Balatro 进程，避免留下悬挂 session。
- 更新研究记忆、发现、开放问题和本运行日志。

## 验证结果（Verification）

- `python3 -m unittest discover -s tests`：105 个测试通过。
- `python3 -m balatro_agent summarize-eval --log-dir runs/eval/live-20260607-agent2-manual-wait`：
  - `run_count: 1`
  - `win_count: 0`
  - `loss_count: 0`
  - `status: incomplete`
  - `error_count: 2`
  - `failure_phase: ROUND_EVAL`
  - `final_ante: 3`
  - `final_score: 2790`
  - `final_required_score: 0`
- Lovely 日志记录 `functions/common_events.lua:1220: attempt to index field 'round_eval' (a nil value)`。

## 新发现（New Findings）

- 当前 AGENT2 手动等待 run 在 ante 3 small blind 达到 2790/2000 后，被 Lovely `round_eval` nil 崩溃截断，不能用于判定通关概率提升。
- 崩溃前 AGENT2 仍是 `j_clever`、`j_hack`、`j_mystic_summit`、`j_sly`、`j_droll` 的弱满槽构筑，且 ante 3 前未获得稳定倍率或 X 倍率 Joker。
- live eval 仍会在 active run 回到 `MENU` 后进入 fallback `gamestate` 循环，导致不可判读日志目录；后续应把这类情况记为 `infra_error`。

## 失败原因（Failure Cause）

- 未达成通关，也未得到完整失败局。直接原因是基础设施中断：`cash_out` 后 Lovely/Balatro 在结算 UI 触发 `round_eval` nil 崩溃。
- 策略侧仍有未解决风险：AGENT2 在 ante 3 继续携带弱 chip Joker 满槽，没有证据表明当前候选能越过既有的 4000 分关口。
- 证据来源：`runs/eval/live-20260607-agent2-manual-wait/AGENT2.jsonl`、`summarize-eval` 输出和 Lovely 日志。

## 记忆更新（Memory Updates）

- 更新 `research/memory.md`：记录 AGENT2 手动等待 run 的不可判读结果、2790/2000 的有限信号、以及 `round_eval` nil 崩溃风险。

## 决策更新（Decision Updates）

无。

## 开放问题（Open Questions）

- `Runner.run` 是否应把 active run 中的 `MENU` 回退、连续超时和 `round_eval` nil 崩溃统一记录为 `infra_error`？
- AGENT2 弱满槽时，是否应把低等级星球牌购买让位给现金保留、目标 reroll 或卖弱 Joker 找倍率/X 倍率？
- 当前已有未提交策略候选是否应拆分为可回归验证的最小改动，避免多项启发式同时污染 AGENT2 判读？

## 下次成功率估计（Next Success Estimate）

- 下一轮若先加固 live eval 基础设施并只验证 AGENT2，估计得到一局可判读 AGENT2 结果的概率为 45%-60%；估计 AGENT2 越过 4000 分的概率为 15%-25%。依据：本轮手动等待 run 能越过 2000 分 small blind，但仍是弱满槽构筑，且基础设施连续中断。

## 建议的下一次运行（Recommended Next Run）

先实现或验证 runner 的 `infra_error` 记录：active run 回到 `MENU`、连续 `gamestate` 超时、`cash_out` 连接超时或 Lovely 崩溃时终止 seed 并保存崩溃前进度。随后重启 BalatroBot，单独重跑 AGENT2 到 4000 分关口，再决定是否调整弱 Joker 满槽的卖牌/重掷策略。
